import { ToggleDisableDirective } from './toggle-disable.directive';

describe('ToggleDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDisableDirective();
    expect(directive).toBeTruthy();
  });
});
